// src/app/startAuthListener.js
// Auth listener desactivado por ahora (SplitMate no usa login todavía)
// Esto evita errores en Netlify build.

export const startAuthListener = () => () => {
  // no-op
};
